name: CI Pipeline

on:
  push

env:
  DEBUG: 1

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  test:
    runs-on: ubuntu-latest
    steps:
      - run: make test
  cache:
    runs-on: ubuntu-latest
    steps:
      - run: make push
